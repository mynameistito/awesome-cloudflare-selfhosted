name: audit

on:
  pull_request:
    paths: ['data/**', 'README.md', 'scripts/**', 'package.json']
  schedule:
    - cron: '0 6 1 * *'   # monthly, first of the month
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Test the config parser
        run: npm test

      - name: Check README is generated from entries/
        run: npm run check

      - name: Lint the list against the awesome spec
        run: npm run lint

      - name: Audit entries against GitHub
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run audit
